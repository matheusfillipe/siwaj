# siwaj local secrets. Copy to .env and fill in. Never commit .env.
# Provisioned to the device's encrypted NVS over USB serial: make provision
OPENWEATHER_API_KEY=

# Without these the device finds no network, falls back to an open softAP, and
# can serve its config page but never fetch weather.
WIFI_SSID=
WIFI_PASS=
